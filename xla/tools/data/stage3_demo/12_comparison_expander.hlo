HloModule demo

// ComparisonExpander:
// 将 TOTALORDER 比较展开为基础比较 + sign + select
// 因为大多数后端不支持原生 TOTALORDER 比较
ENTRY main {
  p0 = f32[4] parameter(0)
  p1 = f32[4] parameter(1)
  ROOT compare = pred[4] compare(p0, p1), direction=LT, type=TOTALORDER
}
