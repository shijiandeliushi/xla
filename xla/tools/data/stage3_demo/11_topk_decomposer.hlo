HloModule demo

// TopkDecomposer:
// 将 topk 分解为基础操作序列:
// topk → iota + sort(compare) + slice + gte
// 这是变化最显著的 Pass 之一
ENTRY main {
  p0 = f32[100] parameter(0)
  ROOT topk = (f32[10], s32[10]) topk(p0), k=10, largest=true
}
