HloModule demo

// QrExpander:
// 将 QR 分解 (CustomCall with target="Qr") 展开为 Householder 反射算法
// QR → dot + subtract + divide + 分块迭代
ENTRY main {
  p0 = f32[6,4] parameter(0)
  ROOT qr = (f32[6,4], f32[4,4]) custom-call(p0), custom_call_target="Qr"
}
