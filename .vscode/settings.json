{
    "cmake.sourceDirectory": "/home/xianghonghui/xla/xla/mlir_hlo",
    "clangd.arguments": [
        "--compile-commands-dir=${workspaceFolder}",
        "--background-index",
        "--clang-tidy",
        "--header-insertion=iwyu",
        "--completion-style=detailed",
        // 【关键配置 1】只用 2 个核心建立索引，防止卡死系统
        "-j=2", 
        // 【关键配置 2】定期释放内存，防止内存泄漏
        "--malloc-trim" 
    ],
    // 确保这个也关掉了
    "C_Cpp.intelliSenseEngine": "disabled"
}
